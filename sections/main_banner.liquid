{% for block in section.blocks %}
<div class="slideshow-container">
<div class="mySlides fade">
  <div class="numbertext"><h2>{{ block.settings.slide_caption1 }}</h2>
   <h3>{{ block.settings.slide_caption2 }}</h3>
    <p>{{ block.settings.slide_caption3 }}<br><br>
    <button type="button" class="sliderbutton"><a href="{{ block.settings.url2 }}">SHOP</a></button>
    </p>
  </div>
  <div class="gallery-item">
  <img style="width:100%" src="{{ block.settings.gallery-image | img_url: ''  }}" />
     </div>
  </div>
</div>
  {% endfor %}
<br>
<div style="text-align:center">
  <span class="dot"></span>
  <span class="dot"></span>
  <span class="dot"></span>
</div>
{% schema %}
  {
    "name": "main_banner",
    "settings": [
		{
          "type": "image_picker",
          "id": "image12",
          "label": "add Image"
        }
],
 "blocks": [
     {
       "name": "slide",
       "type": "slider",
       "settings": [
         {
           "type": "image_picker",
           "id": "gallery-image",
           "label": "Add Slide"
         },
{
     "type": "text",
    "id": "slide_caption1",
    "label":"Slide Caption",
     "default":"Add Caption Heading"
},
     {
      "type": "text",
    "id": "slide_caption2",
    "label":"Slide Caption",
     "default":"Add Caption text"
},
 {
              "type": "text",
    "id": "slide_caption3",
    "label":"Slide Caption",
     "default":"Add Caption text"
},
{
    "type": "text",
    "id": "url2",
    "label":"Shop Button Url",
     "default":"Shop Button Url"
}
       ]
     }
   ],
"presets" : [
    {
      "category":"main_banner",
      "name":"main_banner"
    }
]
  }
{% endschema %}
<style>
img {vertical-align: middle;
height: auto;
}
/* Slideshow container */
.slideshow-container {
  max-width: 100%;
  position: relative;
  margin: auto;
}
/* Caption text */
.text {
  color: #000;
  padding: 8px 12px;
  position: absolute;
  bottom: 8px;
  width: 100%;
  text-align: center;
}
/* Number text (1/3 etc) */
.numbertext {
  color: #000;
  padding: 8px 12px;
  position: absolute;
 top: 30%;
    left: 14%;
    border-left: 2px solid #000;
}
.numbertext h2{
  font-size:46px;
  text-transform: uppercase;
    margin-bottom: -20px;
  font-weight: 800;
}
/* The dots/bullets/indicators */
.dot {
  height: 15px;
  width: 15px;
  margin: 0 2px;
  background-color: #bbb;
  border-radius: 50%;
  display: inline-block;
  transition: background-color 0.6s ease;
}
.active {
  background-color: #717171;
}
/* Fading animation */
.fade {
  -webkit-animation-name: fade;
  -webkit-animation-duration: 1.5s;
  animation-name: fade;
  animation-duration: 1.5s;
}
.sliderbutton
  {
      padding-top: 10px;
    padding-bottom: 10px;
    padding-left: 15px;
    padding-right: 15px;
    background: #AF2246;
    color: #fff;
    text-transform: uppercase;
    border: 2px solid #AF2246;
    border-radius: 6px;
    font-size: 18px;
  }
  .sliderbutton a
  {
   color:#fff;
    text-decoration:none;
  }
@media only screen and (max-width: 600px) {
.numbertext h2 {
    font-size:24px;
  }
}
</style>
<script>
var slideIndex = 0;
showSlides();
function showSlides() {
    var i;
    var slides = document.getElementsByClassName("mySlides");
    var dots = document.getElementsByClassName("dot");
    for (i = 0; i < slides.length; i++) {
       slides[i].style.display = "none";
    }
    slideIndex++;
    if (slideIndex > slides.length) {slideIndex = 1}
    for (i = 0; i < dots.length; i++) {
        dots[i].className = dots[i].className.replace(" active", "");
    }
    slides[slideIndex-1].style.display = "block";
    dots[slideIndex-1].className += " active";
    setTimeout(showSlides, 5000);
}
</script>
{% stylesheet %}
{% endstylesheet %}
{% javascript %}
{% endjavascript %}